import type { PostSource } from '@/app/lib/types';

const jpaSummary2: PostSource = {
    slug: 'jpaSummary2', // URL ROUTER 에 사용
    topic: 'JPA',
    title: 'JPA상속',
    description: 'JPA상속',
    publishedAt: '2025-02-15',
    tags: ['JPA'],
    featured: true,
    contentPath: 'jpa/JPA상속.md',
};

export default jpaSummary2;
